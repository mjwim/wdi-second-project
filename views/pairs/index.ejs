<main class='container'>
  <div class="row">
    <ul class='list-group col-12'>
      <div class="row">
        <% pairs.forEach((pair) => { %>
          <li class="hovereffect list-group-item col-lg-4 col-12">
            <img class="col-6" src ="<%= pair.cheese.image %>" alt="<%= pair.cheese.name %>">
            <img class="col-6" src ="<%= pair.wine.image %>" alt="<%= pair.wine.name %>">
            <a href="/pairs/<%= pair.id %>">
              <div class="overlay">
                <h2><%= pair.wine.name %> & <%= pair.cheese.name %></h2>
                <div class="info">
                  <h4><%= pair.createdBy.username %></h4>
                  <p><%= pair.description %></p>
                </div>
              </a>
            </div>
          </li>
        <% }); %>
      </div>
    </ul>
  </div>
</main>
